version: 2

models:
  - name: int_matches_with_odds
    columns:
      - name: match_id
        tests:
          - unique
          - not_null
      - name: odds_event_id
        tests:
          - not_null:
              config:
                severity: warn